import { FileNode, NODE } from './useCodeEditor';

const file: FileNode = {
    id: 'root',
    name: 'root',
    type: NODE.FOLDER,
    children: [
        {
            id: 'app',
            name: 'app',
            type: NODE.FOLDER,
            children: [],
        },
        {
            id,
            content:
        },
        {
            
        }

    ],
};



/path/to/something/file.rs
